replicaCount: 2

egress:
  ws_url: <ws_url>
  api_key: <api_key>
  api_secret: <secret>
  log_level: info
  health_port: 8080
  prometheus_port: 9090
  redis:
    address: <redis_host:port>
  s3:
    access_key: <access_key>
    secret: <secret>
    region: "us-west-2"
    bucket: "my-egress"
