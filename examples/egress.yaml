replicaCount: 2

egress:
  wsUrl: <ws_url>
  apiKey: <api_key>
  apiSecret: <secret>
  logLevel: info
  healthPort: 8080
  prometheusPort: 9090
  redis:
    address: <redis_host:port>
  s3:
    accessKey: <access_key>
    secret: <secret>
    region: "us-west-2"
    bucket: "my-egress"
