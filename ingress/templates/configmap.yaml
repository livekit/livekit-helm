{{- if not .Values.storeSecretsInSecret.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ingress.fullname" . }}
  labels:
    {{- include "ingress.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.ingress | indent 4 }}
{{- end }}
