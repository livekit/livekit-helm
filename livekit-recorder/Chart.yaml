apiVersion: v2
name: livekit-recorder
description: LiveKit recorder is used by LiveKit server to record rooms.
type: application
version: 0.2.0
appVersion: "v0.2.0"

sources:
  - https://github.com/livekit/livekit-recorder
